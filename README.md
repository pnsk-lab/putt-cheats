# Putt Cheats

Userscript build using `vite-plugin-monkey`.

```sh
bun install
bun run build
```

Install the generated script from:

```text
dist/putt.user.js
```

For dev mode:

```sh
bun run dev
```
